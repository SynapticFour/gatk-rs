# 6R.107 test maintenance — HOLDOUT_6R104 / 6R.105

This is **not** a new parity arrow. Production 6R.107 was not modified.

Pinned oracle: GATK **4.4.0.0**, SHA `2dbc025821bc5f686c423ff332a41e6cef892a77`, GKL 0.8.8.

## 1. Why 6R.104 was stale

**File:** `gatk-haplotypecaller/tests/holdout_6r104_holdout_allele_set.rs`  
**Test:** `holdout_6r104_first_allele_set_divergence` (env `HOLDOUT_6R104=1`)

**Original purpose (6R.104 report):** separate inventories

- A — EventMap union
- B — `replaceSpanDels` / merged genotyping alleles
- C — emitted VCF

Java EventMap already had `C/T`. Inventory C was the first unequal object: Java VCF absent, Rust extra `20:29455388 C/T`.

**Stale assertions (pre-6R.107 live snapshot):**

```text
vcf_record_at(rust.vcf, 29455388).expect("Rust VCF C/T")
alleles == "C/T"
calls_at.len() == 1
calls_at[0] is C/T
```

6R.106 proved that extra was L9 `SparsePlShape::Het` (`PL 81,0,36`) after calculator `PL=0,6,1780` failed Java emit. 6R.107 stopped that overwrite for non-HomAltStrong SNP pileup (`44,4` is Het). Inventory C now matches Java: **no VCF record**. EventMap presence is still not emission — those A/B asserts stay.

## 2. Why 6R.105 was stale

**File:** `gatk-haplotypecaller/tests/holdout_6r105_genotype_emit.rs`  
**Test:** `holdout_6r105_genotype_emit_dump` (env `HOLDOUT_6R105=1`)

**Original purpose (6R.105 report):** given alleles `[C, T]`, dump assignment / emit. First unequal object was the GL vector. Emit predicates **agree** given the same GLs. Production change then: NONE.

**Stale assertions (froze the pre-6R.106 Rust genotype):**

```text
!calls.is_empty()   // "Rust must still genotype C/T"
rust_emitted         // "Rust still emits C/T"
gt == [0, 1]
keep == [0, 1]
```

That state was `PL=81,0,36` from L9, not the calculator. After 6R.107, live production has **no genotyped C/T** and does **not emit**, matching Java `calculateGenotypes = null`.

## 3. Test changes

### HOLDOUT_6R104

Kept (original A/B purpose):

- Java VCF absent
- EventMap union contains `C/T`
- `replaceSpanDels` is `C/*` beside `C/T`
- same-REF SNP+* is not 6R.61 colocated merge

**Inverted inventory C** to the proven Java-equivalent:

- frozen Rust VCF has no `C/T`
- live emit window does not contain `29455388`
- covering rust.vcf is not rust-only at `C/T`
- `calls_at` is empty (no genotyped C/T)

### HOLDOUT_6R105

Live path no longer yields a genotyped call, so GLs are taken from the proven calculator vector `PL=0,6,1780` (same contract as `forensic_6r105` / 6R.106), then assignment/emit is applied:

- no live genotyped C/T
- no live emit
- Java and frozen Rust VCF absent
- GT `0/0`, keep `[0]`, emit false at conf 10 and 30, `siteIsMonomorphic`

That is a precise inversion from obsolete `0/1`+emit to the Java-equivalent hom-ref no-call, not a weaker assertion.

## 4. Production changes

**NONE** in this task.

Working-tree production files (`genotype_site_pipeline.rs`, `mod.rs`, `sparse_pl_shape.rs`, `lib.rs`) are the pre-existing 6R.107 HomAltStrong gate restored from stash. They were not edited here.

Java dump scripts were not edited here.

## 5. Verification

| Gate | Result |
|------|--------|
| HOLDOUT_6R104 | **PASS** |
| HOLDOUT_6R105 | **PASS** |
| forensic_6r104 | **PASS** (4) |
| forensic_6r105 | **PASS** (5) |
| HOLDOUT_6R106 | **PASS** |
| forensic_6r106 | **PASS** (6) |
| HOLDOUT_6R107 | **PASS** |
| forensic_6r107 | **PASS** (8) |
| HOLDOUT_6R53 | **PASS** (`java_only=[] rust_only=[]` covering) |
| 6R.43 `chr20_tiny` | **PASS** (runner) |
| package lib | **PASS** (657) |
| workspace lib | **PASS** (16+43+657+6) |

Oracle (not regenerated): frozen `parity/reports/6r43/chr20_tiny/java.vcf` and `rust.vcf` both lack `20:29455388`.

## 6. Final status

**6R.107 VERIFICATION DEBT CLOSED.**

The 6R.107 production change remains unchanged.
The remaining failures were stale test expectations from the pre-6R.106 behavior.
No production parity arrow was reopened.
