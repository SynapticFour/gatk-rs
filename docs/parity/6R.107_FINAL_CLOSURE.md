# 6R.107 — Final closure / verification

This document does **not** replace `docs/parity/6R.107_REPORT.md`. That file is the historical investigation record. This file records the verification debt run after that investigation.

Pinned oracle: GATK **4.4.0.0**, Java source SHA `2dbc025821bc5f686c423ff332a41e6cef892a77`, GKL 0.8.8.

HEAD at verification: `2130b9ef15e8718534037d013a1a84b793227f49` (`restore-seqgraph-control-flow`).

**This session did not change production code.**

## 1. Production change

Implemented in 6R.107 (already on the working tree; not modified during this verification):

```text
l9_may_overwrite_pairhmm_gls_after_emit_fail(event, ref_ad, alt_ad):
  if P12 (contig 2 / chr2): false
  if !genome_wide_genotype_read_support: false
  if SNP: pileup_is_hom_alt_strong(ref_ad, alt_ad)   // existing HomAltStrong class
  else: true   // indel post-emit-fail L9 unchanged
```

Applied only at the post-`finalize_site` L9 boundary in `genotype_site_pipeline.rs`. Empty-mapper / empty-subset L9, `SparsePlShape` tables, and the GATK-compatible GL calculator were not changed.

## 2. Causal proof

At `20:29455388 C/T`:

```text
Java calculateGLsForThisEvent
    → PL 0,6,1780
    → emit fails
    → OLD L9 overwrite (SNP alt_ad >= 1)
    → SparsePlShape::Het
    → PL 81,0,36
    → false C/T emission

fixed:
    HomAltStrong(44,4) == false
    → no overwrite
    → PL 0,6,1780 preserved
    → GT 0/0
    → no emission
```

Reverified live (`HOLDOUT_6R107=1`):

```json
{
  "original_calculator_pl": [0, 6, 1780],
  "pileup_ref": 44,
  "pileup_alt": 4,
  "sparse_from_pileup": "Het",
  "l9_may_overwrite_pairhmm_after_emit_fail": false,
  "live_genotyped": false,
  "rust_emitted": false
}
```

Frozen covering VCF: `20:29455388` absent from both `java.vcf` and `rust.vcf`.

HOLDOUT_6R53 covering assertion still:

```text
java_only=[]
rust_only=[]
```

## 3. Tests

| Gate | Result | Notes |
|------|--------|-------|
| 6R.107 micro-contract (`--test forensic_6r107_l9_overwrite_contract`) | **PASS** | 8/8. Calculator `0,6,1780` fails Java emit; `(44,4)` not HomAltStrong; `(0,4)` and `(1,8)` are HomAltStrong; no-alt false; P12 excluded; indel keeps genome-wide |
| lib unit `pileup_hom_alt_strong` | **PASS** | existing HomAltStrong class |
| HOLDOUT_6R107 | **PASS** | live overwrite false; no genotyped/emitted C/T |
| HOLDOUT_6R53 | **PASS** | covering `java_only=[] rust_only=[]`; `29455388` absent |
| 6R.43 `ctrl_mid_b` | **PASS** | runner exit 0; compare class **A** 3/3 |
| 6R.43 P12 | **PASS** (runner) | see per-region table; L9 already disabled on contig 2 |
| 6R.43 `chr20_w47` | **PASS** (runner) | compare class **C** Stage F (historical allele-set; not 6R.107) |
| 6R.43 `chr21_w10` | **PASS** (runner) | compare class **C** Stage F (historical; 333 vs 257) |
| 6R.43 `chr20_tiny` | **PASS** (runner) | regenerated rust.vcf; `29455388` still absent |
| package lib | **PASS** | 657 passed; 0 failed; 0 ignored; exit 0 |
| workspace lib | **PASS** | completed (previous attempt was infrastructure/disk). gatk-common 16 + gatk-core 43 + gatk-haplotypecaller 657 + gatk-rs-equiv 6. gatk-cli has no `--lib` tests. Exit 0 |
| `cargo fmt --check` | **PASS** | |
| P11 `p11_call_region_vcf_test` | **PASS** | |
| P12 `p12_call_none_mid_b_test --release` | **PASS** | 1 passed |
| forensic 6R.64–6R.107 | **PASS** | all existing `forensic_6r*.rs` with n≥64; 0 fails |
| HOLDOUT_6R51 / 54 / 57 / 58 | **PASS** | |
| HOLDOUT_6R64–103, 106–107 (existing holdout binaries) | **PASS** | missing numbers have no holdout binary |
| HOLDOUT_6R104 | **FAIL** | **test failure**, not 6R.107 production/parity. Asserts `Rust VCF C/T` and live genotyped C/T. That was the 6R.104 snapshot of the then-divergent emit. After HomAltStrong gating, C/T is correctly absent |
| HOLDOUT_6R105 | **FAIL** | **test failure**, same class. Asserts `Rust must still genotype C/T at 20:29455388` |

### 6R.43 remaining regions (this session)

| region | command | exit | Java/Rust compare | classification |
|--------|---------|------|-------------------|----------------|
| `ctrl_mid_b` | `HOLDOUT_6R43=1 HOLDOUT_ONLY=ctrl_mid_b cargo test -p gatk-haplotypecaller --test holdout_6r43_test -- --test-threads=1 --nocapture` | 0 | A, 3/3, java_only=[] rust_only=[] | **PASS** |
| `p12_snp_cluster` | `HOLDOUT_ONLY=p12_snp_cluster` | 0 | A, 9/9 | **PASS** (runner). Compare A (historical matrix was C; later rounds, not 6R.107) |
| `p12_indel_mix` | `HOLDOUT_ONLY=p12_indel_mix` | 0 | C Stage F, java 11 / rust 10, java_only=`2:92307324 TTC/T` | runner **PASS**; compare **C** historical, not chased |
| `p12_mid_a` | `HOLDOUT_ONLY=p12_mid_a` | 0 | C Stage H, alleles 17/17 | runner **PASS**; compare **C** historical FORMAT |
| `p12_post` | `HOLDOUT_ONLY=p12_post` | 0 | A, 20/20 | **PASS** |
| `p12_het_tail` | `HOLDOUT_ONLY=p12_het_tail` | 0 | A, 4/4 | **PASS** |
| `p12_desert` | `HOLDOUT_ONLY=p12_desert` | 0 | A, 0/0 | **PASS** |
| `chr20_w47` | `HOLDOUT_ONLY=chr20_w47` | 0 | C Stage F, 53/52 | runner **PASS**; compare **C** historical |
| `chr21_w10` | `HOLDOUT_ONLY=chr21_w10` | 0 | C Stage F, 333/257 | runner **PASS**; compare **C** historical |

Java oracles (`parity/reports/6r43/*/java.vcf`) were **not** regenerated (mtime still 30 Aug). Rust VCFs were rewritten by the existing holdout runner only.

Compare used existing `scripts/parity/compare_6r43_vcfs.py` with `--json-out /tmp/6r107_6r43_compare.json` (did not overwrite `parity/reports/6r43/compare.json`).

## 4. Disk / infrastructure verification

Filesystem: `/dev/disk2s5` mounted at `/System/Volumes/Data` (repo and `/tmp`).

| | |
|--|--|
| free space before cleanup | **12 GiB** (98% used) |
| space reclaimed | **8.5 GiB** leftover Cursor sandbox `cargo-target` from previous agent hashes `00b648e5…` (2.9G) and `9f2fd7fac…` (5.6G). Contents were only `cargo-target`. Not source, not `parity/`, not oracles |
| free space after cleanup | **20 GiB** |
| workspace `target/` after verification | **4.6 GiB** (debug + release P12) |
| free space at end | **11 GiB** |

Tests were run with `CARGO_TARGET_DIR=/Users/SynapticFour/devel/SynapticFour/gatk-rs/target` (workspace tree), not a new sandbox cache.

**Workspace-wide compilation completed.** The previous `--lib` failure was **infrastructure/resource** (disk exhaustion in sandbox cache), not a parity failure.

HOLDOUT_6R104 / 6R105 are **not** infrastructure failures.

## 5. Regression conclusion

**6R.107 NOT CLOSED — VERIFICATION DEBT REMAINS**

Required gates that actually ran:

- focused 6R.107 contract: PASS
- HOLDOUT_6R107: PASS
- HOLDOUT_6R53: PASS
- remaining 6R.43 panel: runner PASS (historical compare-C on some P12/chr20/chr21 holdouts unchanged in kind; not treated as 6R.107 breakage)
- package lib: PASS
- workspace-wide lib: **PASS (completed this time)**
- fmt / P11 / P12 release: PASS
- HOLDOUT_6R51 / 54 / 57 / 58: PASS
- forensic 6R.64–107: PASS
- HOLDOUT_6R104: **FAIL**
- HOLDOUT_6R105: **FAIL**

Those two failures are **test failures**: live dumps from 6R.104 / 6R.105 still require the pre-gate rust-only C/T emit. They do **not** prove the HomAltStrong gate is wrong. They were not weakened. Production was not changed.

## 6. Next-arrow status

6R.107 remains open.

Do not begin the next holdout.

No new parity arrow was investigated.

Production code should not be touched to make HOLDOUT_6R104 / 6R105 green. Those tests still encode the phenotype 6R.107 was written to stop. Repointing them to the 6R.107 overwrite contract (as 6R.106 already was) is a separate, explicit test-maintenance decision — not done in this verification pass.

## Commands actually run

1. `git status --short` / `git branch --show-current` / `git rev-parse HEAD`
2. `df -h .` / `df -h /tmp` / `du -sh` of `target`, `~/.cargo`, `~/.rustup`, `/tmp`
3. Inspected leftover `/var/folders/…/cursor-sandbox-cache/*/cargo-target`
4. Removed two leftover sandbox `cargo-target` dirs (8.5G)
5. `cargo test -p gatk-haplotypecaller --test forensic_6r107_l9_overwrite_contract -- --test-threads=1`
6. `cargo test -p gatk-haplotypecaller --lib pileup_hom_alt_strong -- --test-threads=1`
7. `HOLDOUT_6R107=1 cargo test -p gatk-haplotypecaller --test holdout_6r107_l9_overwrite -- --test-threads=1 --nocapture`
8. `HOLDOUT_6R53=1 cargo test -p gatk-haplotypecaller --test holdout_6r53_chr20_tiny_test -- --test-threads=1 --nocapture`
9. Remaining 6R.43: `HOLDOUT_6R43=1 HOLDOUT_ONLY=<id>` for `ctrl_mid_b`, each `p12_*`, `chr20_w47`, `chr21_w10`
10. `python3 scripts/parity/compare_6r43_vcfs.py --panel scripts/parity/6r43_holdout_panel.json --out-root parity/reports/6r43 --json-out /tmp/6r107_6r43_compare.json`
11. `cargo test -p gatk-haplotypecaller --lib -- --test-threads=1`
12. `cargo test --lib -- --test-threads=1`
13. `cargo fmt --check`
14. `cargo test -p gatk-haplotypecaller --test p11_call_region_vcf_test -- --test-threads=1`
15. `cargo test -p gatk-haplotypecaller --test forensic_6r*` for n≥64
16. `cargo test -p gatk-haplotypecaller --test p12_call_none_mid_b_test --release -- --test-threads=1`
17. `HOLDOUT_6R43=1 HOLDOUT_ONLY=chr20_tiny cargo test -p gatk-haplotypecaller --test holdout_6r43_test -- --test-threads=1 --nocapture`
18. `HOLDOUT_6R<n>=1 cargo test -p gatk-haplotypecaller --test holdout_6r<n>_*` for n in {51,54,57,58} ∪ existing 64–107 holdout binaries
