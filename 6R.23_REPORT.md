# 6R.23 REPORT — Why k=85 ALT Threading Produces a REF-Disconnected Island

Canonical mid-B: contig 2, active 92317262–92317491, padded REF start 92316662 length 1430, locus 92317399 C→A. Pinned Java GATK 4.4.0.0 SHA `2dbc025821bc5f686c423ff332a41e6cef892a77`. Java executable unavailable.

Evidence tags: **OBSERVED RUST** / **OBSERVED JAVA SOURCE** / **INFERENCE** / **UNKNOWN — JAVA EXECUTABLE UNAVAILABLE**.

## 1. VERDICT

**OBSERVED RUST:** The k=85 C/A island is disconnected because **none of the 96 read 85-mers equals any padded-REF 85-mer**. Both reads are a 132 bp mate pair (hard-clipped 118H/132M) carrying a **dense local haplotype** (13 and 10 mismatches vs REF), not a lone C→A. The longest stretch of consecutive matching bases is **far below k=85**, so `findStart` begins at unique ALT-only k-mer 0, `threadSequence` never merges into a REF vertex, and `extendChainByOne` never adds a REF↔ALT edge.

**OBSERVED JAVA SOURCE:** The same predicates (`isThreadingStart` = unique k-mer; `getOrCreateKmerVertex` / `getKmerVertex` byte identity; merge only on unique k-mer hit) produce a disconnected chain whenever a read shares **no** k-mer with REF.

**No Rust graph-construction divergence is proven.** Do not patch dangling, k, unique-kmer gates, or inject C/A.

## 2. GRAPH TOPOLOGY

**OBSERVED RUST** (RAW k=85, prune factor 2, **before** dangling):

| | |
|---|---|
| Graph | 1406 nodes, 1404 edges, low_complexity=false |
| Unique / non-unique k-mers | 1350 / 28 |
| Prune | **0** edges removed |
| C/A undirected component | **60** vertices, path-like, **0 REF nodes** |
| Component k-mers in REF 85-mer set | **0 / 60** |

The 60 vertices **are** the union of the two reads’ ALT-only 85-mers (48+48 placements, 60 distinct). Head source and tail sink are the 6R.22 dangling pair.

## 3. REF/ALT BOUNDARY

**OBSERVED RUST:**

There is **no shared k-mer**, so there is no graph edge at a biological SNP boundary. Expected REF 85-mers **do exist** on the REF spine:

| Label | REF offset (pad) | In graph | REF node |
|---|---|---|---|
| Immediately before a lone-C/A window (`off_CA - 85`) | 652 | yes | yes |
| First REF 85-mer overlapping 92317399 | 653 | yes | yes |
| First REF 85-mer after 92317412 | 751 | yes | yes |

They are not joined to the ALT island because **no threaded sequence ever steps from a REF k-mer to an ALT k-mer**. `extend_chain_by_one` only inserts an edge for consecutive k-mers of one sequence.

First read 85-mer vs REF at the same genomic start:

- Read 0 @ 92317300: **10** substitutions; `keys_eq=false` (sequences differ; not an encoding miss).
- Read 1 @ 92317312: **6** substitutions; compact prefixes look identical, diffs are internal.

## 4. THREADING TRACE

**OBSERVED RUST** (`start_threading_only_at_existing_vertex=false`, HQ minQ=10, one HQ segment per read = full 132 bp):

```
READ 0  qname=H06HDADXX130110:1:1101:10040:45938  132M118H  aln=92317300  seq_len=132
  FIND_START = 0  start_in_ref_set=false  start_is_ref_node=false
  THREAD: ALT@0 -> ALT@47   (48 vertices, 0 REF)
  never branches from REF; never rejoins REF

READ 1  same qname  118H132M  aln=92317312  seq_len=132
  FIND_START = 0  start_in_ref_set=false  start_is_ref_node=false
  THREAD: ALT@0 -> ALT@47   (48 vertices, 0 REF)
```

**Read k-mer vs REF set:** n=96, identical=0, alt_only=96, reverse-complement hits on REF=0.

Mismatches vs padded REF (1-based):

- Read 0 (**13**): 92317302 T/A, 304 C/T, 309 A/G, 311 C/T, 326 C/T, 333 C/T, 347 T/C, 357 G/C, 361 C/T, 371 C/G, **399 C/A**, **407 T/C**, **412 G/C**
- Read 1 (**10**): 326 C/T, 333 C/T, 347 T/C, 357 G/C, 361 C/T, 371 C/G, **399 C/A**, **407 T/C**, **412 G/C**, 442 C/G

Largest matching run: read 0 **27** bp, read 1 **29** bp (**≪ 85**), so a REF 85-mer on these sequences is impossible.

## 5. START-VERTEX / K-MER IDENTITY AUDIT

**OBSERVED JAVA SOURCE** (`AbstractReadThreadingGraph.findStart` ~200–212, `ReadThreadingGraph.isThreadingStart` ~108–110):

- REF sequences start at 0.
- Reads: first `i` with `i < stop - k` where `!nonUniqueKmers.contains(kmer)` (when `startThreadingOnlyAtExistingVertex=false`, which HC sets because `recoverDanglingBranches=true`).
- `Kmer.equals` is **raw byte equality** (hash then `Utils.equalRange`). **No reverse-complement, no canonical orientation.**

**OBSERVED RUST** (`read_threading_graph.rs`):

- `find_start` / `is_threading_start` match those loops (`start..stop.saturating_sub(k)`).
- k=85 **> 64** → `KmerKey::Bytes` (same byte identity as Java `Kmer`).
- Same-window key computed twice: **0 mismatches**.
- RC of read 85-mers vs REF set: **0 hits**.
- `get_or_create_kmer_vertex` / `get_unique_kmer_vertex(allow_ref_source=false)` match Java `getOrCreateKmerVertex` / `getNextKmerVertexForChainExtension`.

**INFERENCE:** Start at 0 is correct: the first 85-mer is unique and not already a REF vertex. Creating a new chain is what Java does for a unique non-REF k-mer.

Minor source delta, **not causal here:** Java `baseIsUsableForAssembly` rejects `N`; Rust `is_base_usable` allows `N` with high quality. Both reads are ACGT, one full-length HQ segment.

## 6. MULTI-SNP CONTROL

Same k=85 builder, test-only synthetic reads = **full padded REF** with substitutions. **OBSERVED RUST:**

| Control | identical_to_REF | alt_only | first_alt..last_alt | island REF nodes | REF-connected |
|---|---|---|---|---|---|
| lone C→A @ 737 | 1261 | 85 | 653..737 | 1346 | **yes** (bubble) |
| C→A + T→C | 1253 | 93 | 653..745 | 1346 | **yes** |
| C→A + T→C + G→C | 1248 | 98 | 653..750 | 1346 | **yes** |
| **real 2 reads** | **0** | **96** | 0..47 | **0** | **no** |

**OBSERVED RUST:** The three oracle SNPs alone, on a long REF-context sequence, **do** form a REF-connected bubble at k=85 (85 / 93 / 98 ALT-only k-mers, as expected for k and SNP span). The real island is caused by **additional flanking mismatches + 132 bp clipped length**, not by C→A+T→C+G→C in isolation.

## 7. JAVA 4.4 COMPARISON

Only **OBSERVED JAVA SOURCE** (SHA `2dbc0258`):

| Operation | Java | Rust (this graph) |
|---|---|---|
| REF then reads pending | `addSequence` / `addRead` then `buildGraphIfNecessary` | `ANONYMOUS_SAMPLE` then `SAMPLE` in `IndexMap` |
| HQ split | `addRead` maximal Q≥min and not N | `sequences_from_read` (N allowed; unused here) |
| Non-unique | per-sequence duplicate k-mers | same |
| `findStart` | first unique k-mer, excluding last k-mer | same; both reads start=0 |
| Vertex identity | `Kmer` byte equality | `KmerKey::Bytes` byte equality at k=85 |
| Chain extend | suffix match on out-edges else unique-k-mer merge else `createVertex` + `addEdge` | same |
| Merge into REF | requires an exact unique k-mer already in `kmerToVertexMap` | 0 such hits on these reads |

**UNKNOWN — JAVA EXECUTABLE UNAVAILABLE:** Java was not executed on this BAM window. We do **not** use the oracle VCF as proof of a Java intermediate haplotype or graph.

## 8. PARITY CLASSIFICATION

**A — Java-equivalent** for read-threading construction on these sequences: a read with **zero** k-mer identity to REF becomes a disconnected path. That is the Java 4.4 algorithm, not a Rust bug.

Not **C**: no proven start-vertex, hash, orientation, or edge-insertion divergence.

Residual **UNKNOWN — JAVA EXECUTABLE UNAVAILABLE:** we did not dump a Java `ReadThreadingGraph` DOT. The construction predicates above do not require SW or other runtime oracles.

## 9. EARLIEST CAUSAL SITE

**Not a divergence.** Earliest **Rust causal site** (why *this* graph is disconnected):

`ReadThreadingGraphBuilder::thread_sequence` → `find_start` returns 0 on an ALT-only unique 85-mer → `extend_chain_by_one` never hits `get_unique_kmer_vertex` for a REF k-mer.

Biological input: 132 bp hard-clipped mates with mismatch spacing **< k**.

6R.22 dangling rejection remains Java-equivalent for the resulting topology.

## 10. NEXT STEP

Do **not** connect the island, lift k=10/25 unique-kmer gates, or inject C/A.

If the goal is still “why does the Java oracle VCF contain 92317399 C/A?”, that is a **different contract** than this graph: **UNKNOWN** without a Java executable (or another independently proven input: e.g. whether Java’s active region contained more/longer reads). Do not treat the VCF as an intermediate haplotype.

Smallest isolated probe if Java becomes available: dump Java k=85 unique-k-mer overlap of these two SEQ strings vs REF; expect **0**, then `removePathsNotConnectedToRef` drops the chain.

Test: `six_r23_k85_alt_island_threading_audit` (structural facts only; does not assert Java must merge).

Production algorithm: **unchanged**.
