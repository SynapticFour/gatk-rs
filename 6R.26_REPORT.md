# 6R.26 REPORT — Production-gated Java-faithful reference k-mer gate

Pinned Java: GATK **4.4.0.0** (`broadinstitute/gatk:4.4.0.0` / SHA `2dbc025821bc5f686c423ff332a41e6cef892a77`).

## 1. VERDICT

**Success — production `createGraph` now uses the extended-region reference haplotype.**

Canonical mid-B production assembly:

| Step | Result |
|---|---|
| Java-faithful extended REF | **430 bp**, `2:92317162–92317591` |
| k=10 uniqueness | **reject** (non-unique) |
| k=25 uniqueness | **accept** |
| Graph | k=25, 430M haplotypes |
| ALT | two 430M ALTs carry `92317399 C/A`, `92317407 T/C`, `92317412 G/C` |
| EventMap | those three sites plus extras `92317361 C/T`, `92317371 C/G` |
| `allowNonUniqueKmersInRef` | **false** (unchanged) |
| P12 injection | **not used** for these ALTs |

No P12-specific assembly injection, allele insert, or coordinate special case was added.

`call_region` still returns `None` at mid-B. That is **pre-existing** (same `p12_call_none_mid_b` failure without this change). 6R.26 does not stack trim/engine fixes.

## 2. Java source basis

**OBSERVED JAVA SOURCE** at SHA `2dbc0258` (fetched; not copied from the 6R.25 report):

`ReadThreadingAssembler.createGraph`:

```java
if ( !allowNonUniqueKmersInRef && !ReadThreadingGraph.determineNonUniqueKmers(
        new ReadThreadingGraph.SequenceForKmers("ref", refHaplotype.getBases(), 0,
                refHaplotype.getBases().length, 1, true), kmerSize).isEmpty() ) {
    return null;
}
rtgraph.addSequence("ref", refHaplotype.getBases(), 1, true);
```

Uniqueness and `addSequence("ref", …)` use the **same** `refHaplotype.getBases()`.

`AssemblyBasedCallerUtils.assembleReads`:

```java
fullReferenceWithPadding = region.getAssemblyRegionReference(referenceReader, REFERENCE_PADDING_FOR_ASSEMBLY); // ±500
paddedReferenceLoc = getPaddedReferenceLoc(region, REFERENCE_PADDING_FOR_ASSEMBLY, referenceReader);
refHaplotype = createReferenceHaplotype(region, paddedReferenceLoc, referenceReader);
assemblyEngine.runLocalAssembly(region, refHaplotype, fullReferenceWithPadding, paddedReferenceLoc, …);
```

`createReferenceHaplotype` → `ReferenceConfidenceModel.createReferenceHaplotype(region, region.getAssemblyRegionReference(referenceReader), paddedReferenceLoc)`:

**OBSERVED JAVA SOURCE** `AssemblyRegion.getAssemblyRegionReference(reader)` is padding **0** (delegates to the two-arg form with `padding == 0`). That call returns bases of **`paddedSpan`** (extended/overlap region), not ±500.

`createReferenceHaplotype` sets `alignmentStartHapwrtRef = paddedSpan.start - paddedReferenceLoc.start` (offset into the ±500 window). Haplotype **bases** remain the padding-0 span.

`allowNonUniqueKmersInRef` default remains false. Configured k = 10, 25.

**OBSERVED JAVA SOURCE** `findBestPaths`: SW CIGAR vs `refHaplotype.getBases()`; then `h.setAlignmentStartHapwrtRef(refHaplotype.getAlignmentStartHapwrtRef())`.

**OBSERVED JAVA SOURCE** `EventMap.processCigarForInitialEvents`: `refPos = haplotype.getAlignmentStartHapwrtRef()` into the `ref[]` passed by `buildEventMapsForHaplotypes` (Java uses `fullReferenceWithPadding` + alignment 500).

**OBSERVED LIVE JAVA** (6R.24 Docker, same BAM/interval): k=10 skipped (non-unique), k=25 used, EventMap `{2:92317399 C/A, 2:92317407 T/C, 2:92317412 G/C}`.

## 3. Before/after Rust reference representations

| Object | Before 6R.26 | After 6R.26 |
|---|---|---|
| Active | 92317262–92317491 | unchanged |
| Extended | 92317162–92317591 (430 bp) | unchanged |
| `assembly_reference_read` (±500) | 92316662–92318091 (**1430 bp**) | **still constructed** for error correction / HMER collapse |
| `createGraph` uniqueness | 1430 bp padded REF | **430 bp** extended haplotype |
| `addSequence("ref")` / graph seed | 1430 bp | **same 430 bp object** |
| `AssemblyResultSet` EventMap bytes/loc | 1430 bp @ 92316662 | **430 bp @ 92317162** |
| Scoring `padded_reference_start_1based` | ±500 loc | **extended start** (same origin as graph REF) |

**IMPLEMENTED CONTRACT:** uniqueness bytes == graph-seed bytes == EventMap `reference_bases` (one `graph_ref` `AssemblyRead`).

**INFERENCE / wiring choice:** Java EventMap indexes the 1430 bp padded array at alignment 500. Rust EventMap plus `supplemental_indel_variation_events` re-SWs when `hap.len() != ref_bytes.len()`. Feeding 430 bp haps into a 1430 bp EventMap produced **324** spurious events (Failure-C-class mismatch). 6R.26 does **not** change EventMap; it keeps EventMap `ref_bytes` identical to `refHaplotype.getBases()`. For 430M CIGARs this walk is coordinate-equivalent to Java’s 1430-at-offset-500 walk.

Canonical identity (**OBSERVED RUST**):

```
len=430 start=92317162 end=92317591 digest=be35290d28046387
```

## 4. Exact production code changes

| File | Change |
|---|---|
| `assembly_region_finalize.rs` | Shared slice helper; `create_graph_reference_read` = padding-0 extended bases |
| `assembly_based_caller.rs` | `assemble_from_ref_and_reads(&graph_ref, …)`; scoring loc = extended start; `AssemblyResultSet` stores `graph_ref` bytes + extended start; REF hap `alignment_start_hap_wrt_ref = 0` for that EventMap index |
| `event_map.rs` | `#[path]` to 6R.26 tests only |

**Not changed:** EventMap algorithms, k schedule, uniqueness policy (`allow_non_unique_kmers_in_ref` default false), prune/dangling/SW/k-best, P12 injectors (`fix_p12` / `ensure_p12` / W-H1), mapper, PairHMM, genotyping, VCF, preprocessing.

## 5. Canonical graph-selection result

**OBSERVED RUST** `assemble_reads_with_finalized` + `AssembleReadsArgs::default()`:

- kmer **25**
- 4 haplotypes, all **430M**, length 430, alignment 0 vs extended origin
- `AssembledSomeVariation` / `has_variation_for_calling = true`

## 6. k=10 / k=25 uniqueness

| Reference | k=10 unique | k=25 unique |
|---|---|---|
| Extended 430 bp (production graph REF) | false | **true** |
| Padded 1430 bp (Control D diagnostic) | false | **false** |

Control B: duplicating a 25-mer on the extended sequence → k=25 rejected with `allowNonUniqueKmersInRef=false`.

## 7. Graph lifecycle

No extra lifecycle instrumentation in production. **OBSERVED RUST** matches 6R.25’s test-only k=25 SeqGraph on the same 430 bp REF: ALT survives prune / dangling / SeqGraph / k-best (4 haplotypes, two ALTs carry the three SNPs). First-loss Failure A **did not occur**.

## 8. Haplotype results

| Hap | ref? | CIGAR | 92317399 | 92317407 | 92317412 |
|---|---|---|---|---|---|
| 0 | ALT | 430M | A | C | C |
| 1 | ALT | 430M | A | C | C |
| 2 | ALT | 430M | C | T | G |
| 3 | REF | 430M | C | T | G |

Two ALTs carry the Java three-SNP haplotype. Hap 2 is REF-equivalent at those sites. Count is **not** required to be exactly one ALT.

Preprocessing was not modified. Production haplotypes match the 6R.25 test-only 430 bp graph (same 430M / three-SNP ALTs). **No new divergence** classified as read preprocessing vs graph vs path selection for this region.

## 9. EventMap results

**OBSERVED RUST** (closed 6R.19 EventMap, unmodified):

```
92317361 C/T
92317371 C/G
92317399 C/A
92317407 T/C
92317412 G/C
```

The three Java-observed SNPs are present. Extras `92317361 C/T` and `92317371 C/G` are **not** suppressed (6R.25 already saw them).

## 10. Coordinate audit

Extended origin `92317162` + haplotype offset:

- 92317399 → off 237, hap A, REF C
- 92317407 → off 245, hap C, REF T
- 92317412 → off 250, hap C, REF G

`em_start + em_off == site` for each. Not the 1430 bp ±500 index.

## 11. Regression controls

| Control | Result |
|---|---|
| A mid-B | k=10 reject, k=25 accept on extended REF; production k=25 |
| B non-unique extended | duplicate 25-mer → k=25 rejected; uniqueness still exists |
| C unrelated | 5 other ActiveFull in `2:92317000-92319000`; **1** uniqueness-stable (`92318290–92318521`) assembled (k=35, 4 haps, 9 events). **4/5** have the same padded-vs-extended uniqueness class as mid-B (Java-faithful collateral, not a second gate) |
| D old 1430 | padded 1430 bp still non-unique at k=25 (diagnostic only) |
| Identity | uniqueness == seed == EventMap bytes (430 bp digest above) |

Failure D not declared: sibling uniqueness deltas are the intended representation contract applied globally, not a P12 special case. The stable region still rejects k=10 and k=25 on **both** representations and expands k.

## 12. P12 safeguard results

| Item | Result |
|---|---|
| W-H1 module | present, unedited |
| `fix_p12` / `ensure_p12` | present, unedited |
| P12 cluster overlap for mid-B | **false** |
| TTC→T / A→ATG inject | not invoked for this region (`strict_java_assembly`) |
| C/A source | ordinary k=25 graph + EventMap |
| P12-specific 6R.26 code | **none** |

## 13. Test results

```
cargo fmt --all -- --check                         ok
cargo test -p gatk-haplotypecaller --lib six_r26   1 passed
cargo test -p gatk-haplotypecaller --lib six_r     54 passed
cargo test -p gatk-haplotypecaller --lib           511 passed
cargo test -p gatk-haplotypecaller                 lib+most integration ok;
                                                   p12_call_none_mid_b FAILED
```

`p12_call_none_mid_b` expects `call_region` **Some** with the three SNPs. It fails with `call_region None` **with and without** the 6R.26 production diff (**pre-existing**, 6R.20). Not introduced by 6R.26.

## 14. Production diff audit

```
gatk-haplotypecaller/src/assembly_based_caller.rs       | graph_ref wiring
gatk-haplotypecaller/src/assembly_region_finalize.rs    | padding-0 slice helper
gatk-haplotypecaller/src/event_map.rs                   | test #[path] only
tests/event_map/event_map_p12_6r26_test.rs              | new
tests/event_map/event_map_p12_6r25_test.rs              | production pin updated
```

No EventMap algorithm, P12 workaround, mapper, PairHMM, genotyping, VCF, prune/dangling thresholds, k schedule, or uniqueness-policy edits.

## 15. Classification

**IMPLEMENTED CONTRACT:** production `ReadThreadingAssembler` graph boundary uses `getAssemblyRegionReference` padding 0 for uniqueness **and** the reference sequence seed.

**OBSERVED RUST:** canonical pipeline reaches k=25, ALT 430M, EventMap emits the three Java SNPs.

**INFERENCE:** EventMap extras at 92317361/92317371 remain a haplotype/CIGAR vs Java-log difference (6R.25); not suppressed.

**UNKNOWN:** why `call_region` still returns `None` after a non-empty EventMap (trim / engine / genotyping). Out of scope.

## 16. Recommended 6R.27

1. **Engine/trim after a correct EventMap** — `call_region` None at mid-B despite 5 untrimmed events (pre-existing). Identify the first drop (`trimTo`, variation-present, read filter, genotyping). Do not change EventMap.
2. Do **not** lift `allowNonUniqueKmersInRef` or add P12 inject to force a VCF site.
3. Soft-clip preprocessing remains a possible Java/Rust source difference (6R.24/6R.25); only reopen if haplotype CIGARs diverge from the 6R.25 430 bp graph.
4. Quantify Control C sibling regions that now accept k=25 (same representation class) on a holdout slice if a broader uniqueness-gate impact log is needed.
